@{
  Layout = "_Layout";
}

@model TravelPlanner.Models.Destination

<div class="container h-100">
  <div class="row h-100 justify-content-center align-items-center">
    <div class="jumbotron text-center content-box">
      <h2>Are you sure you want to delete this?</h2>

      @Html.DisplayNameFor(model => model.DestinationName): @Html.DisplayFor(model => model.DestinationName)
      @using (Html.BeginForm())
      {
          <br><input type="submit" value="Delete" class="btn btn-primary btn-lg" />
      }
      <p><a class="btn btn-primary btn-lg" href="@Url.Action("Index")" role="button">Back to List</a></p>
    </div>
  </div>
</div>
